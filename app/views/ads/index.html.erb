<html>
<p id="notice"><%= notice %></p>


<head>
</head>

<body>
    <% @ads.each do |ad| %>

        <table class="table table-hover">
          <thead>
          <tr>
            <td>Nome: </td>
            <td>Descrição</td>
            <td>Profissão</td>
            <td>Criado em:</td>
            <td>Actions:</td>
          </tr>
          </thead>
          <div class="content">

            <tbody>
            <tr>
              <td><%= ad.user.name.upcase %></td>
              <td><%= ad.description %></td>
              <td><%= ad.profession.name_m %></td>
              <td><%= created_date ad.created_at %></td>
              <td><%= link_to ad, class: 'btn btn-primary btn-xs' do %>
                    <%= content_tag :span, "", :class => "glyphicon glyphicon-eye-open" %>
                <% end %></td>
                </button>
              </p>
              </td>
            </tr>
          </div>
          </tbody>
        </table>

    <% end %>

</body>
</html>