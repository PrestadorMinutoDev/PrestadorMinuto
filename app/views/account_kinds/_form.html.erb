<%= form_for(@account_kind) do |f| %>
  <% if @account_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_kind.errors.count, "error") %> prohibited this account_kind from being saved:</h2>

      <ul>
      <% @account_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
