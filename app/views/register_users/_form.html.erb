<%= form_for(@register_user) do |f| %>
  <% if @register_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@register_user.errors.count, "error") %> prohibited this register_user from being saved:</h2>

      <ul>
      <% @register_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
